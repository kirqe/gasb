require 'spec_helper'

describe 'AnaliticsRequest' do
  # it "has a list of available modules" do
  #   expect(ServiceRegistry.refs).to be_instance_of(Array)
  # end

  # it "returns an instance according to :kind param" do
  #   expect(ServiceRegistry.select_service(:now)).to be(NowRequest)
  #   expect(ServiceRegistry.select_service(:day)).to be(DayRequest)
  #   expect(ServiceRegistry.select_service(:week)).to be(WeekRequest)
  #   expect(ServiceRegistry.select_service(:month)).to be(MonthRequest)
  # end
end