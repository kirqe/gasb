apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.redis.config.name }}
  labels:
    group: {{ .Values.redis.group }}
data: 
{{- range .Values.global.config.data }}
  {{ .key }}: {{ .value | quote}}
{{- end}}