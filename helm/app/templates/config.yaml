apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.app.config.name }}
  labels:
    group: {{ .Values.app.group }}  
data: 
{{- range .Values.global.config.data }}
  {{ .key }}: {{ .value | quote}}
{{- end}}